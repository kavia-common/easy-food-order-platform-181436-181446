# Backend environment example

# Database URL (async driver). Use asyncpg for Postgres; fallback to SQLite for dev.
# Example Postgres (use values from database container env):
# postgresql+asyncpg://$POSTGRES_USER:$POSTGRES_PASSWORD@localhost:$POSTGRES_PORT/$POSTGRES_DB
DATABASE_URL=sqlite+aiosqlite:///./dev.db

# CORS origin for frontend
FRONTEND_ORIGIN=http://localhost:3000

# Auth
JWT_SECRET=change_me_in_prod
ACCESS_TOKEN_EXPIRE_MINUTES=60
PASSWORD_SALT=devsalt

# Payments (optional)
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
PAYMENT_AUTO_COMPLETE=true
